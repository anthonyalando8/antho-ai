version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.9
  command: python manage.py collectstatic && gunicorn --workers MamaPesa.wsgi
  network:
    port: 8000
